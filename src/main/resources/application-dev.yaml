spring:
    datasource:
        url: jdbc:postgresql://localhost:5432/KFD
        username: user
        password: pass
        driver-class-name: org.postgresql.Driver
    jpa:
        hibernate:
            ddl-auto: create
        show-sql: true

server:
    port: 8083
    servlet:
        context-path: /api

jwt:
    key: "top_secret_key_and_it_has_to_be_longer_than_256_bits"